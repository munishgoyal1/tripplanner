name: Build & Deploy to Azure Container Apps

# Triggers: every push to master + manual run
on:
  push:
    branches: [master]
    paths-ignore:
      - "**/*.md"
      - "data/**"
      - ".gitignore"
  workflow_dispatch:

permissions:
  id-token: write    # OIDC token for Azure login
  contents: read
  packages: write    # Push image to GHCR

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/multiagent
  RESOURCE_GROUP: rg-multiagent-trip-planner

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push image (sha + latest tags)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy infra + app via Bicep
        uses: azure/cli@v2
        with:
          inlineScript: |
            az deployment group create \
              --resource-group "$RESOURCE_GROUP" \
              --template-file infra/main.bicep \
              --parameters infra/main.bicepparam \
              --query "properties.outputs.containerAppUrl.value" \
              -o tsv
        env:
          # Image tag = commit SHA (immutable, traceable)
          CONTAINER_IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          # Azure OpenAI
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_DEPLOYMENT }}
          AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          # Travel APIs
          DUFFEL_API_KEY: ${{ secrets.DUFFEL_API_KEY }}
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}

      - name: Print app URL
        uses: azure/cli@v2
        env:
          RESOURCE_GROUP: ${{ env.RESOURCE_GROUP }}
        with:
          inlineScript: |
            APP_NAME=$(az containerapp list -g "$RESOURCE_GROUP" --query "[0].name" -o tsv)
            URL=$(az containerapp show -g "$RESOURCE_GROUP" -n "$APP_NAME" \
              --query "properties.configuration.ingress.fqdn" -o tsv)
            echo "App URL: https://$URL"
            {
              echo "## Deployment successful"
              echo ""
              echo "**App URL:** https://$URL"
              echo "**Image:** \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}\`"
            } >> "$GITHUB_STEP_SUMMARY"
